[project]
name = "prometheus-libmanager-exporter"
version = "0.1.0"
authors = [ { name = "Dustin Frisch", email = "fooker@lab.sh" } ]

requires-python = ">=3.13"
dependencies = [
    "click >= 8.1",
    "prometheus-client >= 0.20",
    "requests >= 2.30",
    "beautifulsoup4 >= 4.12",
    "structlog >= 25.4",
]

[project.scripts]
prometheus-libmanager-exporter = "prometheus_libmanager_exporter.__main__:main"

[build-system]
requires = ["hatchling >= 1.27.0"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]

[tool.ruff]
target-version = "py313"
line-length = 140

